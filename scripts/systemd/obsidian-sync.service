[Unit]
Description=Obsidian Sync headless daemon
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=obsidian
Group=obsidian
WorkingDirectory=/srv/obsidian
Environment=HOME=/srv/obsidian
Environment=XDG_CONFIG_HOME=/srv/obsidian/.config
Environment=XDG_DATA_HOME=/srv/obsidian/.local/share
# The remote vault is bound to this local path once via `ob sync-setup
# --vault <name> --path /srv/obsidian/vault`; `ob sync` then takes --path, not --vault.
ExecStart=/usr/bin/ob sync --continuous --path /srv/obsidian/vault
Restart=on-failure
RestartSec=5

# --- Sandbox ---
NoNewPrivileges=yes
ProtectSystem=strict
ProtectHome=yes
PrivateTmp=yes
ReadWritePaths=/srv/obsidian
ProtectKernelTunables=yes
ProtectKernelModules=yes
ProtectControlGroups=yes
RestrictAddressFamilies=AF_INET AF_INET6
RestrictNamespaces=yes
CapabilityBoundingSet=
AmbientCapabilities=
RestrictSUIDSGID=yes
LockPersonality=yes

[Install]
WantedBy=multi-user.target
